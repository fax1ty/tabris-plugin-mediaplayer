# Tabris.JS MediaPlayer Plugin

## Example

```ts
let mediaPlayer = new MediaPlayer({ url: 'urlHere' });
mediaPlayer.on('ready', ()=> mediaPlayer.play());
```

## Android

1. Doesn't work in the background yet


## iOS

iOS maintainer is needed.